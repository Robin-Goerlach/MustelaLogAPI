<Window x:Class="MustelaLog.Client.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MustelaLog.Client.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Width="1680"
        Height="980"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="Refresh" Click="Refresh_Click" />
                <Separator />
                <Button Content="Send test log event" Click="SendTestLog_Click" />
                <Button Content="Open diagnostics" Click="OpenDiagnostics_Click" />
                <Separator />
                <Button Content="Export CSV" Click="ExportCsv_Click" />
                <Button Content="Export JSON" Click="ExportJson_Click" />
                <Separator />
                <TextBlock VerticalAlignment="Center" Margin="6,0,4,0" Text="Time mode:" />
                <ComboBox x:Name="TimeModeComboBox" Width="100" SelectionChanged="TimeModeCombo_SelectionChanged">
                    <ComboBoxItem>Local</ComboBoxItem>
                    <ComboBoxItem>Utc</ComboBoxItem>
                </ComboBox>
                <Separator />
                <TextBlock VerticalAlignment="Center" Margin="6,0,4,0" Text="Saved view:" />
                <ComboBox Width="180" ItemsSource="{Binding SavedViews}" DisplayMemberPath="Name" SelectedValuePath="Name" SelectedValue="{Binding SelectedSavedViewName, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Width="160" Margin="6,0,0,0" Text="{Binding SavedViewEditName, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Save" Click="SaveView_Click" />
                <Button Content="Load" Click="LoadView_Click" />
                <Button Content="Rename" Click="RenameView_Click" />
                <Button Content="Delete" Click="DeleteView_Click" />
            </ToolBar>
        </ToolBarTray>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="{Binding ConnectionStatus}" />
            <Separator />
            <StatusBarItem Content="{Binding StatusMessage}" />
            <Separator />
            <StatusBarItem Content="{Binding LastRefreshText, StringFormat=Last refresh: {0}}" />
            <Separator />
            <StatusBarItem Content="{Binding LastResponseText, StringFormat=Last response: {0}}" />
            <Separator />
            <StatusBarItem Content="{Binding TimeModeLabel, StringFormat=Time mode: {0}}" />
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="310" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="450" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="300" />
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.RowSpan="2" Margin="8" Padding="8" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Filters" Margin="0,0,0,12" />
                        <TextBlock Text="Quick range" />
                        <DockPanel Margin="0,4,0,8">
                            <ComboBox Width="180" ItemsSource="{Binding QuickRanges}" SelectedItem="{Binding SelectedQuickRange, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Content="Apply" Margin="8,0,0,0" Click="ApplyQuickRange_Click" />
                        </DockPanel>
                        <TextBlock Margin="0,0,0,4" FontSize="11" Foreground="#666666" Text="Die Datumsfelder arbeiten tagesbasiert. Minuten- und Stunden-Schnellfilter werden in V1 bewusst auf diesen tagesbasierten UI-Ansatz abgebildet." TextWrapping="Wrap" />

                        <TextBlock Text="From" />
                        <DatePicker SelectedDate="{Binding FromDateLocal, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="To" />
                        <DatePicker SelectedDate="{Binding ToDateLocal, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,12,0,0" Text="Source" />
                        <TextBox Text="{Binding SourceKeyFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Hostname" />
                        <TextBox Text="{Binding HostnameFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Service" />
                        <TextBox Text="{Binding ServiceFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Severity" />
                        <TextBox Text="{Binding SeverityFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Category" />
                        <TextBox Text="{Binding CategoryFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Action" />
                        <TextBox Text="{Binding ActionFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Outcome" />
                        <TextBox Text="{Binding OutcomeFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Text search" />
                        <TextBox Text="{Binding TextSearchFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Correlation ID" />
                        <TextBox Text="{Binding CorrelationFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Trace ID" />
                        <TextBox Text="{Binding TraceFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Request ID" />
                        <TextBox Text="{Binding RequestFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Component" />
                        <TextBox Text="{Binding ComponentFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Actor User ID" />
                        <TextBox Text="{Binding ActorUserFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Actor Principal" />
                        <TextBox Text="{Binding ActorPrincipalFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Session Hash" />
                        <TextBox Text="{Binding SessionFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Client IP" />
                        <TextBox Text="{Binding ClientIpFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0" Text="Server IP" />
                        <TextBox Text="{Binding ServerIpFilter, UpdateSourceTrigger=PropertyChanged}" />
                        <CheckBox Margin="0,10,0,0" IsChecked="{Binding OnlyWithPayload}" Content="Only events with payload" />
                        <CheckBox Margin="0,4,0,0" IsChecked="{Binding OnlyWithCorrelation}" Content="Only events with correlation / trace" />
                        <CheckBox Margin="0,4,0,0" IsChecked="{Binding OnlyWithActor}" Content="Only events with actor" />
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Content="Apply Filters" Click="Refresh_Click" />
                            <Button Content="Clear" Margin="8,0,0,0" Click="ClearFilters_Click" />
                        </StackPanel>
                        <TextBlock FontWeight="SemiBold" Margin="0,14,0,6" Text="Active filters" />
                        <ItemsControl ItemsSource="{Binding ActiveFilterChips}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,6,6" Padding="6,3" CornerRadius="10" Background="#EEF5FF" BorderBrush="#D7E8FF" BorderThickness="1">
                                        <TextBlock Text="{Binding}" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock FontWeight="SemiBold" Margin="0,14,0,6" Text="Visible columns" />
                        <ItemsControl ItemsSource="{Binding ColumnOptions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ColumnOptionViewModel}">
                                    <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsVisible}" Margin="0,2,0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Grid Grid.Column="1" Grid.Row="0" Margin="8,8,4,4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" FontSize="18" FontWeight="SemiBold" Text="Events" Margin="0,0,0,8" />
                <Border Grid.Row="1" Margin="0,0,0,8" Padding="8" Background="#FFF7E8" BorderBrush="#F0D8A8" BorderThickness="1" CornerRadius="4" Visibility="{Binding LocalFilteringNotice, Converter={StaticResource StringHasTextToVisibilityConverter}}">
                    <TextBlock Text="{Binding LocalFilteringNotice}" TextWrapping="Wrap" />
                </Border>
                <DataGrid x:Name="EventsDataGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserResizeColumns="True" CanUserSortColumns="True" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" SelectionChanged="EventsDataGrid_SelectionChanged" Sorting="EventsDataGrid_Sorting" ItemsSource="{Binding EventRows}" SelectedItem="{Binding SelectedEvent, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Occurred At" Binding="{Binding OccurredAtDisplay}" SortMemberPath="occurredAt" Width="150" />
                        <DataGridTextColumn Header="Ingested At" Binding="{Binding IngestedAtDisplay}" Width="150" CanUserSort="False" />
                        <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" SortMemberPath="severityNumber" Width="100" />
                        <DataGridTextColumn Header="Source / Host" Binding="{Binding SourceOrHost}" SortMemberPath="sourceKey" Width="180" />
                        <DataGridTextColumn Header="Service" Binding="{Binding Service}" Width="140" CanUserSort="False" />
                        <DataGridTextColumn Header="Category" Binding="{Binding EventCategory}" Width="120" CanUserSort="False" />
                        <DataGridTextColumn Header="Action" Binding="{Binding EventAction}" Width="130" CanUserSort="False" />
                        <DataGridTextColumn Header="Outcome" Binding="{Binding EventOutcome}" Width="100" CanUserSort="False" />
                        <DataGridTextColumn Header="Message" Binding="{Binding MessagePreview}" Width="300" CanUserSort="False" />
                        <DataGridTextColumn Header="Correlation / Trace" Binding="{Binding CorrelationOrTracePreview}" Width="150" CanUserSort="False" />
                        <DataGridTextColumn Header="Event ID" Binding="{Binding EventId}" Width="190" CanUserSort="False" />
                    </DataGrid.Columns>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Foreground" Value="{Binding Severity, Converter={StaticResource SeverityBrushConverter}}" />
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
                <DockPanel Grid.Row="3" Margin="0,8,0,0">
                    <TextBlock VerticalAlignment="Center" Text="{Binding PageSummary}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                        <Button Content="Previous" Click="PreviousPage_Click" Margin="0,0,6,0" />
                        <Button Content="Next" Click="NextPage_Click" />
                    </StackPanel>
                </DockPanel>
            </Grid>

            <Border Grid.Column="2" Grid.Row="0" Margin="4,8,8,4" Padding="8" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="6">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Event Details" Margin="0,0,0,8" />
                        <TextBlock FontWeight="SemiBold" Text="General" />
                        <Grid Margin="0,4,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Event ID:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEvent.Record.LogEventId}" IsReadOnly="True" />
                            <TextBlock Grid.Row="1" Text="Occurred At:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEvent.OccurredAtDisplay}" IsReadOnly="True" />
                            <TextBlock Grid.Row="2" Text="Observed At:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEvent.ObservedAtDisplay}" IsReadOnly="True" />
                            <TextBlock Grid.Row="3" Text="Received At:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedEvent.ReceivedAtDisplay}" IsReadOnly="True" />
                            <TextBlock Grid.Row="4" Text="Ingested At:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedEvent.IngestedAtDisplay}" IsReadOnly="True" />
                            <TextBlock Grid.Row="5" Text="Severity:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding SelectedEvent.Record.SeverityText}" IsReadOnly="True" />
                            <TextBlock Grid.Row="6" Text="Event Name:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding SelectedEvent.Record.EventName}" IsReadOnly="True" />
                            <TextBlock Grid.Row="7" Text="Category / Action / Outcome:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding SelectedCategoryActionOutcome}" IsReadOnly="True" />
                        </Grid>
                        <TextBlock FontWeight="SemiBold" Text="Source" />
                        <Grid Margin="0,4,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Source Name:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEvent.Record.SourceName}" IsReadOnly="True" />
                            <TextBlock Grid.Row="1" Text="Source Key:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEvent.Record.SourceKey}" IsReadOnly="True" />
                            <TextBlock Grid.Row="2" Text="Host:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEvent.Record.HostName}" IsReadOnly="True" />
                            <TextBlock Grid.Row="3" Text="Service:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedEvent.Record.ServiceName}" IsReadOnly="True" />
                            <TextBlock Grid.Row="4" Text="Component:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedEvent.Record.ComponentName}" IsReadOnly="True" />
                            <TextBlock Grid.Row="5" Text="Version / Env:" FontWeight="SemiBold" />
                            <TextBlock Grid.Row="5"
           Grid.Column="1"
           Text="{Binding SelectedSourceVersionAndEnvironment}" />
                        </Grid>
                        <TextBlock FontWeight="SemiBold" Text="Actor / Context" />
                        <Grid Margin="0,4,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Actor User:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEvent.Record.ActorUserId}" IsReadOnly="True" />
                            <TextBlock Grid.Row="1" Text="Principal:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEvent.Record.ActorPrincipal}" IsReadOnly="True" />
                            <TextBlock Grid.Row="2" Text="Session Hash:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEvent.Record.SessionHashSha256}" IsReadOnly="True" />
                            <TextBlock Grid.Row="3" Text="Client IP:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedEvent.Record.ClientIp}" IsReadOnly="True" />
                            <TextBlock Grid.Row="4" Text="Server IP:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedEvent.Record.ServerIp}" IsReadOnly="True" />
                        </Grid>
                        <TextBlock FontWeight="SemiBold" Text="Correlation" />
                        <Grid Margin="0,4,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Trace ID:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEvent.Record.TraceId}" IsReadOnly="True" />
                            <TextBlock Grid.Row="1" Text="Span ID:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEvent.Record.SpanId}" IsReadOnly="True" />
                            <TextBlock Grid.Row="2" Text="Correlation ID:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEvent.Record.CorrelationId}" IsReadOnly="True" />
                            <TextBlock Grid.Row="3" Text="Request ID:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedEvent.Record.RequestCorrelationId}" IsReadOnly="True" />
                        </Grid>
                        <TextBlock FontWeight="SemiBold" Text="Message / Payload" />
                        <TextBlock Margin="0,4,0,0" Text="Message" FontWeight="SemiBold" />
                        <TextBox Text="{Binding SelectedEvent.Record.MessageText}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" Height="60" />
                        <TextBlock Margin="0,8,0,0" Text="Attributes JSON" FontWeight="SemiBold" />
                        <TextBox Text="{Binding SelectedEventPrettyAttributes, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto" />
                        <TextBlock Margin="0,8,0,0" Text="Raw Payload JSON" FontWeight="SemiBold" />
                        <TextBox Text="{Binding SelectedEventPrettyPayload}" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto" />
                        <TextBlock FontWeight="SemiBold" Margin="0,8,0,0" Text="Governance / Integrity" />
                        <Grid Margin="0,4,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Classification:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEvent.Record.ClassificationCode}" IsReadOnly="True" />
                            <TextBlock Grid.Row="1" Text="Retention:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEvent.Record.RetentionPolicyCode}" IsReadOnly="True" />
                            <TextBlock Grid.Row="2" Text="Legal Hold:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEvent.Record.LegalHoldFlag}" IsReadOnly="True" />
                            <TextBlock Grid.Row="3" Text="Hash / Chain:" FontWeight="SemiBold" />
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedIntegritySummary}" IsReadOnly="True" AcceptsReturn="True" Height="70" TextWrapping="Wrap" />
                        </Grid>
                        <WrapPanel Margin="0,10,0,0">
                            <Button Content="Copy Event ID" Click="CopyEventId_Click" Margin="0,0,6,6" />
                            <Button Content="Copy Message" Click="CopyMessage_Click" Margin="0,0,6,6" />
                            <Button Content="Copy JSON" Click="CopyJson_Click" Margin="0,0,6,6" />
                            <Button Content="Copy Correlation ID" Click="CopyCorrelation_Click" Margin="0,0,6,6" />
                            <Button Content="Copy Trace ID" Click="CopyTrace_Click" Margin="0,0,6,6" />
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <TabControl Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Margin="8,4,8,8">
                <TabItem Header="Related Events">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Button Content="By Correlation ID" Click="RelatedCorrelation_Click" Margin="0,0,6,0" />
                            <Button Content="By Trace ID" Click="RelatedTrace_Click" Margin="0,0,6,0" />
                            <Button Content="By Request ID" Click="RelatedRequest_Click" Margin="0,0,6,0" />
                            <Button Content="By Source" Click="RelatedSource_Click" Margin="0,0,6,0" />
                            <Button Content="By Session" Click="RelatedSession_Click" Margin="0,0,6,0" />
                            <Button Content="By Actor" Click="RelatedActor_Click" />
                        </StackPanel>
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding RelatedEventsTitle}" FontWeight="SemiBold" Margin="0,0,0,6" TextWrapping="Wrap" />
                            <DataGrid Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding RelatedEventRows}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Occurred At" Binding="{Binding OccurredAtDisplay}" Width="150" />
                                    <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="100" />
                                    <DataGridTextColumn Header="Source / Host" Binding="{Binding SourceOrHost}" Width="180" />
                                    <DataGridTextColumn Header="Service" Binding="{Binding Service}" Width="140" CanUserSort="False" />
                                    <DataGridTextColumn Header="Message" Binding="{Binding MessagePreview}" Width="350" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </TabItem>

                <TabItem Header="Aggregations">
                    <Grid Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <GroupBox Grid.Column="0" Grid.Row="0" Header="By Severity" Margin="4">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding SeverityAggregation}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Severity" Binding="{Binding Key}" Width="*" />
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                        <GroupBox Grid.Column="1" Grid.Row="0" Header="By Source / Host" Margin="4">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding SourceAggregation}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Source / Host" Binding="{Binding Key}" Width="*" />
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                        <GroupBox Grid.Column="2" Grid.Row="0" Header="By Service" Margin="4">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding ServiceAggregation}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Service" Binding="{Binding Key}" Width="*" />
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                        <GroupBox Grid.Column="0" Grid.Row="1" Header="By Category" Margin="4">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding CategoryAggregation}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Category" Binding="{Binding Key}" Width="*" />
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                        <GroupBox Grid.Column="1" Grid.Row="1" Header="By Outcome" Margin="4">
                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding OutcomeAggregation}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Outcome" Binding="{Binding Key}" Width="*" />
                                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                        <GroupBox Grid.Column="2" Grid.Row="1" Header="Over Time (current page)" Margin="4">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding TimeAggregation}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="0,2,0,2">
                                                <TextBlock Width="90" Text="{Binding Label}" />
                                                <ProgressBar Minimum="0" Maximum="{Binding DataContext.TimeAggregationMaxCount, RelativeSource={RelativeSource AncestorType=GroupBox}}" Height="14" Value="{Binding Count}" Margin="8,0,8,0" Width="180" />
                                                <TextBlock Width="40" Text="{Binding Count}" />
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Margin="0,8,0,0" Text="V1 aggregiert die aktuell geladene Treffermenge clientseitig. Spätere serverseitige Aggregations-Endpunkte können hier ohne UI-Neuschnitt ergänzt werden." TextWrapping="Wrap" />
                            </StackPanel>
                        </GroupBox>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
